Quick Start
===========

Before continuing, make sure you have installed OverleafFS. See
:doc:`install` for installation instructions.

1. Launch the app using ``python -m overleaf_fs`` or your installed shortcut.
2. When prompted, choose a root directory where OverleafFS will store all of your profiles.
3. Log in to Overleaf through the embedded browser:

   - The Overleaf login page will appear in a small window (see :ref:`Connecting to Overleaf <connecting_to_overleaf>` for details).
   - Sign in using your normal Overleaf credentials.
   - After logging in, click **Use this login** and choose whether to save the login cookie for future sessions.

4. Create folders and drag projects into them to organize your work.
5. Double‑click a project to open it directly in Overleaf in your default web browser.
6. Pin important projects for quick access.